import { EventChangeRequest } from './event-change-request';

describe('EventChangeRequest', () => {
  it('should create an instance', () => {
    expect(new EventChangeRequest()).toBeTruthy();
  });
});
